<?xml version="1.0" encoding="utf-8"?>
<!-- Backup-Regeln ab Android 12 (android:dataExtractionRules).

     Das Cloud-Backup bleibt bewusst AN: Der Lernfortschritt liegt in
     fwapp.sqlite im Documents-Verzeichnis, und den soll ein Gerätewechsel
     nicht kosten.

     Ausgeschlossen wird nur FlutterSharedPreferences.xml. Dort legt
     supabase_flutter die komplette Session als JSON ab — inklusive
     Refresh-Token (Key `sb-<host>-auth-token`). Ohne diesen Ausschluss
     wandert der Token ins Google-Konto und auf jedes wiederhergestellte
     Gerät. Preis: Server-URL, Anon-Key, Sync-Schalter, Design und
     Wochenziel müssen nach einem Gerätewechsel neu gesetzt werden. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="FlutterSharedPreferences.xml"/>
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="FlutterSharedPreferences.xml"/>
    </device-transfer>
</data-extraction-rules>
