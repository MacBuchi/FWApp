import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // TODO: add real widget tests once ProviderScope + DB can be mocked.
    expect(true, isTrue);
  });
}
